/**
 * @type IProducts
 * * Interfaz de tipo para la estructura de datos de un Producto.
 */
export type IProducts = {id: number, name: string, description: string, price: number, category: string, marca: string};
